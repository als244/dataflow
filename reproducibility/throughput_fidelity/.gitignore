# a run writes EVERYTHING under results/ (settable: run_experiment --results)
results/
# superseded results kept locally as evidence
attic/
# legacy pre-results layouts (older checkouts wrote outputs at top level)
env.json
cells.json
logs/
traces/
figs/
data/
REPORT.md
