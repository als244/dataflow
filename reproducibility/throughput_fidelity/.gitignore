# results are regenerated per box; keep the code in git, not the artifacts
env.json
cells.json
logs/
traces/
figs/
data/
# the written report names the machines it ran on; publishing one is a
# deliberate act (git add -f), not a side effect of running the sweep
REPORT.md
